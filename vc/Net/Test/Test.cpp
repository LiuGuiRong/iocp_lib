#include "stdafx.h"
#include <math.h>
#include <Windows.h>
#pragma comment (lib, "Winmm.lib") 

int _tmain(int argc, _TCHAR* argv[])
{
	volatile bool b = true;

	for (int x = 1; x != 1; x ++) {
		int xxx = 1;
	}

	return 0;
}

